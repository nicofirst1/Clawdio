# Blind test round 4 — in-context timbre, forced ranking (TEMPLATE, fill after listener session)

Listener: TBD. Single-blind, shuffled labels, 4 full-mix variants of the SAME ~50s session (calm read/grep → busy writes → a hot Bash burst → calm again) (`eval/blind/round4.html`, key in `eval/blind/answer-key-round4.txt`, generated by `eval/blind/make_round4_clips.py`).

Round 3 (isolated 25s dry-tap loops) found the control (v2.2 noise-tick) confirmed worst via free comment, but all four clips bottomed out at 1/7 on both Likert scales (a floor effect — see `research/blind-round3-2026-08-04.md` conclusion 2) and picked no winner among woodblock/marimba/plink. Round 3's own methodology conclusion: redo timbre selection **in context** (embedded in the full ambient mix, not an isolated tap loop) and **forced ranking only**, no absolute Likert scales for N=1. This round is that redo.

Variants tested (do not fill in label→variant until listener is done):

| Label | Variant (fill from answer-key-round4.txt) | Description |
| ----- | ----------------------------------------- | ----------- |
| W     |                                           |             |
| X     |                                           |             |
| Y     |                                           |             |
| Z     |                                           |             |

Reference — what each variant is:

- **v2.2 control**: the shipped v2.2 mapping in full — noise-tick drops, the original (pre-half-density) pacing knobs, no air-bed cut. Reproduces round 2/3's "white noise and chaos" complaint condition exactly.
- **v2.3 woodblock**: the current shipped v2.3 default — half-density pacing, air-bed cut, damped woodblock modal click drops (round-3's `Q` candidate).
- **v2.3 marimba**: v2.3 pacing/air-bed, drop_timbre="marimba" (round-3's `P` candidate — muted marimba, softer/darker/longer decay than woodblock).
- **v2.3 plink**: v2.3 pacing/air-bed, drop_timbre="plink" (round-3's `R` candidate — water-drop plink, single decaying sine with a small downward pitch droop).

Note: the v2.2 control clip is not loudness-matched to the v2.3 clips (it measured ~3.5dB louder on `ffmpeg -af volumedetect`) — this is intentional and NOT a methodology bug like round 3 would have flagged: the air-bed level cut is itself part of what v2.3 changed and is under test here, unlike round 3's isolated single-timbre taps where loudness was the one variable that had to be held constant to isolate timbre alone.

## Results

| Rank | Label | Variant |
| ---- | ----- | ------- |
| 1    |       |         |
| 2    |       |         |
| 3    |       |         |
| 4    |       |         |

Free comment: **""**

## Conclusions

TBD once results are in. Specifically check:

1. Does the control (v2.2) rank worst again, confirming round 2/3's finding holds in a fresh, in-context, forced-ranking protocol?
2. Is there now a clear winner among woodblock/marimba/plink (round 3 couldn't pick one)?
3. Does the free comment single out density, timbre, air-bed brightness, or something else as the deciding factor?

## Decision → v2.3 (or v2.3.1)

TBD. If a candidate other than woodblock wins clearly, change `AmbientConfig.drop_timbre`'s default in `sonifier.py` and update `research/BRIEF-v2.3.md` accordingly — do not silently leave the shipped default out of sync with what the blind test picked.
